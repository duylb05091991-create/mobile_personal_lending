@startuml dtb-strategy
!include <archimate/Archimate>
title Strategy — Shared-gate capabilities (DTB)
caption VCB Architecture Modeling Language — Daily Transaction Banking trainee sample
top to bottom direction

Motivation_Outcome(out, "Outcome MOT.OUT.01\nBoth rails consume 50M volume AND 10 quantity")
Strategy_Capability(cap1, "Capability STR.CAP.01\nDaily limit enforcement (outer zoom)")
Strategy_Capability(cap2, "Capability STR.CAP.02\nSame-day domestic credit transfer")
Strategy_Capability(cap3, "Capability STR.CAP.03\nTransfer inquiry")

Rel_Realization_Down(out, cap1, "realization")
Rel_Realization_Down(out, cap2, "realization")
Rel_Realization_Down(out, cap3, "realization")

legend bottom
  Nesting thread (text only): Daily limit enforcement → DailyTransactionBanking → LimitService → VolumeCheck AND QuantityCheck AND AtomicReserveRelease → reserve-before-rail
  One product, two rails. Inquiry is a capability, not a second enterprise story.
  Teaching diagram — redraw in Archi for the dossier.
endlegend
@enduml
