[project]
name = "nopbai-personal-loan-capstone"
version = "1.0.0"
description = "Capstone I-11 runtime for Nopbai Mobile Personal Loan (Team 4)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = ["ignore::DeprecationWarning"]
